// This component is no longer used — the tournament-based swarm model
// has been replaced by a flat multi-agent convergence model.

const ConflictEntry = () => {
  return null;
};

export default ConflictEntry;
